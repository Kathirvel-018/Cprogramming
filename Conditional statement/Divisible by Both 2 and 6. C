#include <stdio.h>

int main() {
    int n;
    printf("Enter a number: ");
    scanf("%d", &n);

    if (n % 2 == 0 && n % 6 == 0)
        printf("Divisible by both 2 and 6\n");
    else
        printf("Not divisible by both 2 and 6\n");

    return 0;
}
