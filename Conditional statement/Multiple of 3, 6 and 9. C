#include <stdio.h>

int main() {
    int num;
    printf("Enter number: ");
    scanf("%d", &num);

    if (num % 3 == 0 && num % 6 == 0 && num % 9 == 0)
        printf("Multiple of 3, 6 and 9");
    else
        printf("Not multiple of 3, 6 and 9");

    return 0;
}
