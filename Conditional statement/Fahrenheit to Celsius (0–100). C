#include <stdio.h>

int main() {
    float f, c;
    printf("Enter temperature in Fahrenheit (0-100): ");
    scanf("%f", &f);

    if(f >= 0 && f <= 100) {
        c = (f - 32) * 5 / 9;
        printf("Celsius = %.2f", c);
    } else {
        printf("Out of range");
    }
    return 0;
}
